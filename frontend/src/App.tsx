import React from 'react';
import ReactDOM from 'react-dom'

const App = () => {
  return (
    <div>ja cie nie moge!!!</div>
  )
}

const container = document.getElementById('app')
ReactDOM.render(<App />, container)
  
export default App;
